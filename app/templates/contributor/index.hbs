{{contributor.login}}
<img {{bind-attr src=contributor.avatarUrl}}/>

<br>

{{#each repository in repositories}}
  {{#link-to 'repository' repository.ownerLogin repository.name}}
    {{repository.name}}<br>
  {{/link-to}}
{{/each}}